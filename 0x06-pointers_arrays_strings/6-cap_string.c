#include "main.h"

/**
 * cap_string - capitalizes all words of a string
 * @s: pointer to char
 *
 * Return: string
 */

char *cap_string(char *s)
{
	int i;
	int j;
	char tab[15] = {' ', '\t', '\n', ',', '.', ';', '!', 
				if (s[i - 1] == tab[j])
				{
					s[i] -= 32;
					break;
				}
			}
		}
	}
	return (s);
}
